{
  "name": "example-create-react-app-express",
  "version": "1.0.0",
  "scripts": {
    "start": "node index.js",
    "heroku-postbuild": "cd client && yarn && yarn run build"
  },
  "dependencies": {
    "express": "^4.16.2",
    "mysql": "^2.16.0",
    "nodemon": "^1.18.4"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
